# README images

The `.svg` files here are **branded placeholders** so the README renders nicely
out of the box. Replace each one with a real screenshot (or short GIF) captured
from a vault running Lookout:

| File | What it should show |
| --- | --- |
| `pan-zoom.svg` | A wide Mermaid diagram in its inline frame, mid pan/zoom, toolbar visible. |
| `fullscreen.svg` | A diagram opened full screen with the instrument toolbar and zoom gauge. |
| `table-fullscreen.svg` | A wide table opened full screen, showing its full width. |

Tips:

- Capture in a clean theme so the survey-cyan accent reads clearly.
- If you save real screenshots as `.png`/`.gif`, update the matching image
  paths in the top-level [`README.md`](../../README.md).
- Keep them reasonably sized (long edge ≈ 1280px) so the README stays light.
